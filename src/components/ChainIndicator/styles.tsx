import styled from 'styled-components'
export const Wrapper = styled.span`
  & > svg {
    font-size: 1.08em;
    vertical-align: text-bottom;
    margin-right: 0.15em;
  }
  display: flex;
  align-items: center
}`
