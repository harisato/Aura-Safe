import { createStyles } from '@material-ui/core/styles'

export const styles = createStyles({
  container: {
    marginLeft: '5px',
    marginRight: '10px',
    letterSpacing: '-0.5px',
    flex: 'none',
  },
  icon: {
    maxWidth: 'none',
  },
})
