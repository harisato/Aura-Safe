import styled from 'styled-components'

import Block from 'src/components/layout/Block'

const StyleCard = styled(Block)`
  margin-top: 20px;
`

const TitleNumberStyled = styled.div`
  font-weight: 510;
  font-size: 20px;
  line-height: 26px;
  color: white;
  align-self: start;
`

export { StyleCard, TitleNumberStyled }
