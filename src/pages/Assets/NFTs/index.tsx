import { ReactElement, useState } from 'react'
import styled from 'styled-components'
const Wrap = styled.div``

function NFTs(props): ReactElement {
  const [tab, setTab] = useState(0)
  return <Wrap></Wrap>
}

export default NFTs
